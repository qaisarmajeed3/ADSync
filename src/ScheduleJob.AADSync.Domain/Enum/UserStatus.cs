namespace  ScheduleJob.Domain.Enum
{
    /// <summary>
    /// Enum for listing user status.
    /// </summary>
    public enum UserStatus
    {
        /// <summary>
        /// User status when user is active.
        /// </summary>
        Active = 0,

        /// <summary>
        /// User status when user is inactive.
        /// </summary>
        Inactive = 1,
    }
}